{
  "name": "wishlist-assmt4",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @wishlist/web",
    "dev:worker": "npm run dev --workspace @wishlist/worker",
    "lint": "npm run lint --workspace @wishlist/worker && npm run lint --workspace @wishlist/web",
    "test": "npm run test --workspace @wishlist/worker",
    "build": "npm run build --workspace @wishlist/worker && npm run build --workspace @wishlist/web"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
